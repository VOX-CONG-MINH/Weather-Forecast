name: data
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain
  shared:
    path: ../shared

  dartz:
  http:
  cloud_firestore:
  firebase_auth:
  shared_preferences: ^2.0.15

  # DI
  get_it:
  injectable:

  freezed_annotation:
  json_annotation:
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner:
  freezed:
  json_serializable:
  injectable_generator:
